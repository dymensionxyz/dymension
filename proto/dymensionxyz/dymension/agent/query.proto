syntax = "proto3";
package dymensionxyz.dymension.agent;

import "gogoproto/gogo.proto";
import "google/api/annotations.proto";
import "cosmos/base/query/v1beta1/pagination.proto";
import "dymensionxyz/dymension/agent/agent.proto";
import "dymensionxyz/dymension/agent/feedback.proto";

option go_package = "github.com/dymensionxyz/dymension/v3/x/agent/types";

service Query {
  // Params queries the module parameters.
  rpc Params(QueryParamsRequest) returns (QueryParamsResponse) {
    option (google.api.http).get = "/dymensionxyz/dymension/agent/params";
  }

  // Agent queries a single agent by id.
  rpc Agent(QueryAgentRequest) returns (QueryAgentResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/agents/{agent_id}";
  }

  // Agents queries all registered agents, paginated.
  rpc Agents(QueryAgentsRequest) returns (QueryAgentsResponse) {
    option (google.api.http).get = "/dymensionxyz/dymension/agent/agents";
  }

  // AgentActions queries an agent's attested action log, paginated.
  rpc AgentActions(QueryAgentActionsRequest)
      returns (QueryAgentActionsResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/agents/{agent_id}/actions";
  }

  // AgentAction queries a single action log entry by (agent_id, seq).
  rpc AgentAction(QueryAgentActionRequest) returns (QueryAgentActionResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/agents/{agent_id}/actions/{seq}";
  }

  // RevokedPolicies queries all revoked policy fingerprints.
  rpc RevokedPolicies(QueryRevokedPoliciesRequest)
      returns (QueryRevokedPoliciesResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/revoked-policies";
  }

  // PolicyRevoked queries whether a policy fingerprint is revoked.
  rpc PolicyRevoked(QueryPolicyRevokedRequest)
      returns (QueryPolicyRevokedResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/revoked-policies/{fingerprint}";
  }

  // AgentReputation queries an agent's aggregate reputation. Zero-valued when
  // no feedback exists.
  rpc AgentReputation(QueryAgentReputationRequest)
      returns (QueryAgentReputationResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/agents/{agent_id}/reputation";
  }

  // AgentFeedback queries a single feedback record by (agent_id, client).
  rpc AgentFeedback(QueryAgentFeedbackRequest)
      returns (QueryAgentFeedbackResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/agents/{agent_id}/feedbacks/{client}";
  }

  // AgentFeedbacks queries all feedback records for an agent, paginated.
  rpc AgentFeedbacks(QueryAgentFeedbacksRequest)
      returns (QueryAgentFeedbacksResponse) {
    option (google.api.http).get =
        "/dymensionxyz/dymension/agent/agents/{agent_id}/feedbacks";
  }
}

message QueryParamsRequest {}

message QueryParamsResponse {
  Params params = 1 [ (gogoproto.nullable) = false ];
}

message QueryAgentRequest { string agent_id = 1; }

message QueryAgentResponse {
  Agent agent = 1 [ (gogoproto.nullable) = false ];
  // fingerprint is the lowercase hex SHA-256 of the deterministic proto
  // marshaling of the agent's policy; pass it to MsgRevokePolicy to revoke.
  string fingerprint = 2;
  // revoked reports whether the agent's policy fingerprint is revoked.
  bool revoked = 3;
}

message QueryAgentsRequest {
  cosmos.base.query.v1beta1.PageRequest pagination = 1;
}

message QueryAgentsResponse {
  repeated Agent agents = 1 [ (gogoproto.nullable) = false ];
  cosmos.base.query.v1beta1.PageResponse pagination = 2;
}

message QueryAgentActionsRequest {
  string agent_id = 1;
  cosmos.base.query.v1beta1.PageRequest pagination = 2;
}

message QueryAgentActionsResponse {
  repeated ActionLogEntry actions = 1 [ (gogoproto.nullable) = false ];
  cosmos.base.query.v1beta1.PageResponse pagination = 2;
}

message QueryAgentActionRequest {
  string agent_id = 1;
  uint64 seq = 2;
}

message QueryAgentActionResponse {
  ActionLogEntry action = 1 [ (gogoproto.nullable) = false ];
}

message QueryAgentReputationRequest { string agent_id = 1; }

message QueryAgentReputationResponse {
  Reputation reputation = 1 [ (gogoproto.nullable) = false ];
  // average_score is score_sum / count via integer division; 0 when count is 0.
  uint32 average_score = 2;
}

message QueryAgentFeedbackRequest {
  string agent_id = 1;
  string client = 2;
}

message QueryAgentFeedbackResponse {
  Feedback feedback = 1 [ (gogoproto.nullable) = false ];
}

message QueryAgentFeedbacksRequest {
  string agent_id = 1;
  cosmos.base.query.v1beta1.PageRequest pagination = 2;
}

message QueryAgentFeedbacksResponse {
  repeated Feedback feedbacks = 1 [ (gogoproto.nullable) = false ];
  cosmos.base.query.v1beta1.PageResponse pagination = 2;
}

message QueryRevokedPoliciesRequest {}

message QueryRevokedPoliciesResponse { repeated string fingerprints = 1; }

message QueryPolicyRevokedRequest { string fingerprint = 1; }

message QueryPolicyRevokedResponse { bool revoked = 1; }
